Use sandbox instead of experiment, because sandbox is more iteractive in nature

Contiously ask for feedback

Add temperature as settings

Implement RAG, user can upload main document, and other texbooks and other references, and have additional settings 
like pages 

Embeeding, chunk the book, send to an vecotr database (chroma deb), query it at runtime to fetch relevant content 
to promote grounded generation. 

see RAG as Q&A on set of pdfs first (demo), use it as tool in our pipline
The experiment is generating the same experiment each time. So see how to make simulations 
like the prompt they showed here
